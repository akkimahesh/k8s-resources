
apiVersion: v1
kind: Pod
metadata:
  name: call-configmap
  namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80  #only for documentation purpose like EXPOSE 80 in Dockerfile
    envFrom:
      - configMapRef:
          name: configmap