apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: roboshop
spec:
  type: LoadBalancer
  selector:
    project: roboshop-demo
    environment: dev
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
      nodePort: 30080